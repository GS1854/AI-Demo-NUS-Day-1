# Snip CLI

A tiny, zero-dependency Node CLI for working with the Snip backend.

## Commands

```bash
snip add <url>
snip ls
snip open <code>
snip help
```

The CLI reads the backend URL from `SNIP_API`, defaulting to `http://localhost:3000`.
